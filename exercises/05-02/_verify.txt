# 과제 05-02 채점기 검증 — 정답·오답 파일을 grader/check.py 에 넣은 출력 원문
# 낼 파일 이름: 관리대장_수식지도.xlsx

## 정답 파일: solution/관리대장_수식지도.xlsx  (끝 번호 0 — 0 이어야 한다)
$ python -X utf8 grader/check.py 05-02 exercises/05-02/solution/관리대장_수식지도.xlsx
과제 05-02 · 수식 칸에만 회색을 칠한 수식 지도 사본

  ✓ 정산 B3:D5(부서별 수식 아홉 칸)에 채우기 색이 있다
  ✓ 정산 B7(매출합계 합계 수식)에 채우기 색이 있다
  ✓ 정산 D7(정산액 합계 수식)에 채우기 색이 있다
  ✓ 「대장」 시트의 값·수식이 원본과 같다(색만 칠했다)
  ✓ 「기준」 시트의 값·수식이 원본과 같다(색만 칠했다)
  ✓ 「정산」 시트의 값·수식이 원본과 같다(색만 칠했다)
  ✓ 「월별」 시트의 값·수식이 원본과 같다(색만 칠했다)
  ✓ 「담당자」 시트의 값·수식이 원본과 같다(색만 칠했다)
  ✓ 「백업_0402」 시트의 값·수식이 원본과 같다(색만 칠했다)
  ✓ 「읽어주세요」 시트의 값·수식이 원본과 같다(색만 칠했다)
  ✓ practice 폴더의 원본 「부서 관리대장.xlsx」는 처음 받은 그대로다

맞은 항목 11 / 11
전부 맞았습니다.

채점기가 못 보는 것 — 스스로 확인하세요:
  □ 수식이 없는 칸에는 새로 칠한 색이 없다 — F5 [이동 옵션] - [수식]으로 고른 칸 수(상태 표시줄)가 칠한 칸 수와 같다
  □ practice 폴더 원본 파일 속성의 「수정한 날짜」가 작업 전과 같다

## 오답 파일: wrong/관리대장_수식지도.xlsx  (끝 번호 1 — 1 이어야 한다)
# 겨눈 실수: 정산 합계 줄(B7·D7)의 수식 칸을 칠하지 않았다
# 그래서 ✗ 여야 하는 항목만 ✗: 예 (✗ 2개 중 겨눈 것 2개)
$ python -X utf8 grader/check.py 05-02 exercises/05-02/wrong/관리대장_수식지도.xlsx
과제 05-02 · 수식 칸에만 회색을 칠한 수식 지도 사본

  ✓ 정산 B3:D5(부서별 수식 아홉 칸)에 채우기 색이 있다
  ✗ 정산 B7(매출합계 합계 수식)에 채우기 색이 있다
      → 정산!B7 에 채우기 색이 없습니다.
  ✗ 정산 D7(정산액 합계 수식)에 채우기 색이 있다
      → 정산!D7 에 채우기 색이 없습니다.
  ✓ 「대장」 시트의 값·수식이 원본과 같다(색만 칠했다)
  ✓ 「기준」 시트의 값·수식이 원본과 같다(색만 칠했다)
  ✓ 「정산」 시트의 값·수식이 원본과 같다(색만 칠했다)
  ✓ 「월별」 시트의 값·수식이 원본과 같다(색만 칠했다)
  ✓ 「담당자」 시트의 값·수식이 원본과 같다(색만 칠했다)
  ✓ 「백업_0402」 시트의 값·수식이 원본과 같다(색만 칠했다)
  ✓ 「읽어주세요」 시트의 값·수식이 원본과 같다(색만 칠했다)
  ✓ practice 폴더의 원본 「부서 관리대장.xlsx」는 처음 받은 그대로다

맞은 항목 9 / 11
✗ 표시한 항목을 고친 뒤 다시 채점해 보세요.

채점기가 못 보는 것 — 스스로 확인하세요:
  □ 수식이 없는 칸에는 새로 칠한 색이 없다 — F5 [이동 옵션] - [수식]으로 고른 칸 수(상태 표시줄)가 칠한 칸 수와 같다
  □ practice 폴더 원본 파일 속성의 「수정한 날짜」가 작업 전과 같다
