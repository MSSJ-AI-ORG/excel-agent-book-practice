# 과제 07-02 채점기 검증 — 정답·오답 파일을 grader/check.py 에 넣은 출력 원문
# 낼 파일 이름: 07-02_날짜.xlsx

## 정답 파일: solution/07-02_날짜.xlsx  (끝 번호 0 — 0 이어야 한다)
$ python -X utf8 grader/check.py 07-02 exercises/07-02/solution/07-02_날짜.xlsx
과제 07-02 · 제목 병합은 풀고 채우지 않기, 글자 날짜는 날짜로

  ✓ 대장 1·2행 — 제목은 A1·A2 한 칸에만 있고, 옆 칸은 비어 있다(채우지 않았다)
  ✓ 대장 B:F열은 그대로다
  ✓ 대장!A3 머리글은 그대로다
  ✓ 대장!A12 — 비어 있던 줄은 그대로 비어 있다
  ✓ 「날짜검산」 시트가 있다
  ✓ 날짜검산!B1 — 날짜로 읽히는 칸 수를 수식으로 센다
  ✓ 날짜검산!B1 — A4:A19 에서 날짜로 읽히는 칸 수 = 날짜가 적혀 있던 칸 수
  ✓ practice 폴더의 원본 「부서 관리대장.xlsx」는 처음 받은 그대로다

맞은 항목 8 / 8
전부 맞았습니다.

채점기가 못 보는 것 — 스스로 확인하세요:
  □ 대장 A1 을 눌렀을 때 A1 한 칸만 선택된다(병합이 풀렸다). A2 도 같다
  □ A4:A19 의 날짜가 칸 오른쪽에 붙고, 원래 글자와 같은 날짜다(예: 「2026.01.22」 → 2026-01-22)
  □ 점으로 찍은 날짜를 바꾼 건수(2건)를 따로 받았다
  □ 날짜로 바꿀 수 없는 값이 있으면 고치지 않고 행 번호와 원래 값만 목록으로 받았다(이 파일에는 없다는 답)

## 오답 파일: wrong/07-02_날짜.xlsx  (끝 번호 1 — 1 이어야 한다)
# 겨눈 실수: 점으로 찍은 날짜(2건)를 글자로 둔 채 넘겼다
# 그래서 ✗ 여야 하는 항목만 ✗: 예 (✗ 1개 중 겨눈 것 1개)
$ python -X utf8 grader/check.py 07-02 exercises/07-02/wrong/07-02_날짜.xlsx
과제 07-02 · 제목 병합은 풀고 채우지 않기, 글자 날짜는 날짜로

  ✓ 대장 1·2행 — 제목은 A1·A2 한 칸에만 있고, 옆 칸은 비어 있다(채우지 않았다)
  ✓ 대장 B:F열은 그대로다
  ✓ 대장!A3 머리글은 그대로다
  ✓ 대장!A12 — 비어 있던 줄은 그대로 비어 있다
  ✓ 「날짜검산」 시트가 있다
  ✓ 날짜검산!B1 — 날짜로 읽히는 칸 수를 수식으로 센다
  ✗ 날짜검산!B1 — A4:A19 에서 날짜로 읽히는 칸 수 = 날짜가 적혀 있던 칸 수
      → 날짜검산!B1 에 15 이(가) 있어야 하는데 13 이(가) 있습니다. (원본 대장 A4:A19 에서 글자가 적힌 칸을 채점기가 직접 센 값 — 이 칸이 모두 날짜로 읽을 수 있는 글자라는 것은 _expected.py 가 확인했다)
  ✓ practice 폴더의 원본 「부서 관리대장.xlsx」는 처음 받은 그대로다

맞은 항목 7 / 8
✗ 표시한 항목을 고친 뒤 다시 채점해 보세요.

채점기가 못 보는 것 — 스스로 확인하세요:
  □ 대장 A1 을 눌렀을 때 A1 한 칸만 선택된다(병합이 풀렸다). A2 도 같다
  □ A4:A19 의 날짜가 칸 오른쪽에 붙고, 원래 글자와 같은 날짜다(예: 「2026.01.22」 → 2026-01-22)
  □ 점으로 찍은 날짜를 바꾼 건수(2건)를 따로 받았다
  □ 날짜로 바꿀 수 없는 값이 있으면 고치지 않고 행 번호와 원래 값만 목록으로 받았다(이 파일에는 없다는 답)
