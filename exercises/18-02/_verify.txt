# 18-02 채점기 검증 — 2026-09-22 00:13

## solution
$ python grader/check.py 18-02 exercises/18-02/solution/부서별합계저장.bas
과제 18-02 · 규칙대로, 겹치지 않는 이름으로 새 매크로 받기

  ✓ 새 모듈에 Sub 가 하나 이상 있다
  ✓ R2 — Option Explicit 이 있다
  ✓ R4 — Sub·Function 줄 바로 윗줄이 모두 글자가 있는 주석이다
  ✓ 새 프로시저 이름이 원본 매크로 이름과 겹치지 않는다
  ✓ 새 파일로 저장하는 SaveAs 가 있다
  ✓ SaveAs 앞에 같은 이름 파일이 있는지 보는 Dir( 가 있다
  ✓ 경고를 끄는 줄(DisplayAlerts = False)이 없다
  ✓ 부서 이름을 댈 때 공백을 모두 뺀다(Replace(…, " ", ""))

맞은 항목 8 / 8
전부 맞았습니다.

채점기가 못 보는 것 — 스스로 확인하세요:
  □ 만들기 전에 매크로목록에서 비슷한 일을 하는 것이 있는지 먼저 찾아봤다
  □ 코드를 받기 전에 «무엇을 할 것인지» 단계를 먼저 받았다(아직 코드는 쓰지 마)
  □ 규칙 중 지키지 못한 것이 있으면 왜 못 지켰는지 먼저 말해 달라고 했고, 답을 받았다
  □ 새 매크로를 사본에서 돌려, 결과 파일의 부서별 합이 대장과 맞는지 봤다
  □ 모듈 이름을 프로시저 이름과 다르게 지었다

(돌려준 번호 0)

## wrong
$ python grader/check.py 18-02 exercises/18-02/wrong/부서별합계저장.bas
과제 18-02 · 규칙대로, 겹치지 않는 이름으로 새 매크로 받기

  ✓ 새 모듈에 Sub 가 하나 이상 있다
  ✓ R2 — Option Explicit 이 있다
  ✓ R4 — Sub·Function 줄 바로 윗줄이 모두 글자가 있는 주석이다
  ✓ 새 프로시저 이름이 원본 매크로 이름과 겹치지 않는다
  ✓ 새 파일로 저장하는 SaveAs 가 있다
  ✗ SaveAs 앞에 같은 이름 파일이 있는지 보는 Dir( 가 있다
      → SaveAs 보다 앞에 Dir( 로 같은 이름 파일이 있는지 보는 줄이 없습니다 — 덮어쓰기 전에 멈춰야 합니다(15.3).
  ✓ 경고를 끄는 줄(DisplayAlerts = False)이 없다
  ✓ 부서 이름을 댈 때 공백을 모두 뺀다(Replace(…, " ", ""))

맞은 항목 7 / 8
✗ 표시한 항목을 고친 뒤 다시 채점해 보세요.

채점기가 못 보는 것 — 스스로 확인하세요:
  □ 만들기 전에 매크로목록에서 비슷한 일을 하는 것이 있는지 먼저 찾아봤다
  □ 코드를 받기 전에 «무엇을 할 것인지» 단계를 먼저 받았다(아직 코드는 쓰지 마)
  □ 규칙 중 지키지 못한 것이 있으면 왜 못 지켰는지 먼저 말해 달라고 했고, 답을 받았다
  □ 새 매크로를 사본에서 돌려, 결과 파일의 부서별 합이 대장과 맞는지 봤다
  □ 모듈 이름을 프로시저 이름과 다르게 지었다

(돌려준 번호 1)

practice 원본 여섯 파일 지문이 MANIFEST 와 같은가: True
