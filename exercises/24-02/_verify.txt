── 정답 파일 ──
$ python -X utf8 grader/check.py 24-02 exercises/24-02/solution/out/정산.csv
과제 24-02 · 정산액과 반올림, 규칙은 코드 밖에

  ✓ 머리글 줄이 「부서,정산액」이다
  ✓ 「영업1팀」 줄 = 79,000
  ✓ 「영업2팀」 줄 = 173,000
  ✓ 「영업3팀」 줄 = 389,000
  ✓ 「합계」 줄 = 641,000

맞은 항목 5 / 5
전부 맞았습니다.

채점기가 못 보는 것 — 스스로 확인하세요:
  □ rules.json 의 값마다 원래 엑셀 어디에서 왔는지(이름 정의·시트·칸)가 적혀 있다
  □ 기준 시트의 적용요율 열이 정산 수식에서 안 쓰인다는 사실이 rules.json 주석으로 남아 있고, 계산은 지금 수식 그대로 했다
  □ 반올림을 고친 뒤 세 부서 값을 엑셀과 «다시» 대조했다(「고쳤다」가 「맞다」가 아니다)
  □ 엑셀이 낸 정산값 세 줄과 합계를 rules.json 의 검산 칸에 적었고, 돌릴 때마다 대조하게 했다
(끝 번호 0)

── 오답 파일 ──
$ python -X utf8 grader/check.py 24-02 exercises/24-02/wrong/out/정산.csv
과제 24-02 · 정산액과 반올림, 규칙은 코드 밖에

  ✓ 머리글 줄이 「부서,정산액」이다
  ✓ 「영업1팀」 줄 = 79,000
  ✗ 「영업2팀」 줄 = 173,000
      → 「영업2팀,173000」 줄이 없습니다. 숫자는 쉼표 없이, 부서 이름은 대장에 적힌 그대로 정확히 같은 줄만 더합니다.
  ✓ 「영업3팀」 줄 = 389,000
  ✗ 「합계」 줄 = 641,000
      → 「합계,641000」 줄이 없습니다. 숫자는 쉼표 없이, 부서 이름은 대장에 적힌 그대로 정확히 같은 줄만 더합니다.

맞은 항목 3 / 5
✗ 표시한 항목을 고친 뒤 다시 채점해 보세요.

채점기가 못 보는 것 — 스스로 확인하세요:
  □ rules.json 의 값마다 원래 엑셀 어디에서 왔는지(이름 정의·시트·칸)가 적혀 있다
  □ 기준 시트의 적용요율 열이 정산 수식에서 안 쓰인다는 사실이 rules.json 주석으로 남아 있고, 계산은 지금 수식 그대로 했다
  □ 반올림을 고친 뒤 세 부서 값을 엑셀과 «다시» 대조했다(「고쳤다」가 「맞다」가 아니다)
  □ 엑셀이 낸 정산값 세 줄과 합계를 rules.json 의 검산 칸에 적었고, 돌릴 때마다 대조하게 했다
(끝 번호 1)

판정: 기대대로 — 정답은 전부 ✓, 오답은 겨눈 항목만 ✗ (✗ 2개: 「영업2팀」 줄 = 173,000 / 「합계」 줄 = 641,000)
