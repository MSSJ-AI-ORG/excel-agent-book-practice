── 정답 파일 ──
$ python -X utf8 grader/check.py 20-01 exercises/20-01/solution/부서 관리대장_20260922_0002.xlsx
과제 20-01 · 자동 사본이 원본과 같은지 지문으로 확인하기

  ✓ 자동으로 뜬 사본이 원본과 바이트까지 같다(크기가 아니라 지문으로 댔다)
  ✓ practice 의 원본 「부서 관리대장.xlsx」는 처음 받은 그대로다

맞은 항목 2 / 2
전부 맞았습니다.

채점기가 못 보는 것 — 스스로 확인하세요:
  □ 사본 이름이 「원래이름_숫자8자리_숫자4자리.xlsx」 모양이다(날짜와 시각까지 들어 있다) — 채점기는 아직 파일 이름을 보지 못합니다
  □ 드라이런 뒤에 「파일이 실제로 바뀌었는지」를 따로 확인했고, 대장 B5 가 「영업 1팀」 그대로였다
  □ 「안 고쳤습니다」라는 답을 확인으로 쓰지 않았다 — 파일 수정 시각과 B열 값을 직접 받았다
(끝 번호 0)

── 오답 파일 ──
$ python -X utf8 grader/check.py 20-01 exercises/20-01/wrong/부서 관리대장_20260922_0002.xlsx
과제 20-01 · 자동 사본이 원본과 같은지 지문으로 확인하기

  ✗ 자동으로 뜬 사본이 원본과 바이트까지 같다(크기가 아니라 지문으로 댔다)
      → 제출한 파일이 practice/부서 관리대장.xlsx 와 바이트까지 같아야 하는데 다릅니다.
  ✓ practice 의 원본 「부서 관리대장.xlsx」는 처음 받은 그대로다

맞은 항목 1 / 2
✗ 표시한 항목을 고친 뒤 다시 채점해 보세요.

채점기가 못 보는 것 — 스스로 확인하세요:
  □ 사본 이름이 「원래이름_숫자8자리_숫자4자리.xlsx」 모양이다(날짜와 시각까지 들어 있다) — 채점기는 아직 파일 이름을 보지 못합니다
  □ 드라이런 뒤에 「파일이 실제로 바뀌었는지」를 따로 확인했고, 대장 B5 가 「영업 1팀」 그대로였다
  □ 「안 고쳤습니다」라는 답을 확인으로 쓰지 않았다 — 파일 수정 시각과 B열 값을 직접 받았다
(끝 번호 1)

판정: 기대대로 — 정답은 전부 ✓, 오답은 겨눈 항목만 ✗ (✗ 1개: 자동으로 뜬 사본이 원본과 바이트까지 같다(크기가 아니라 지문으로 댔다))
