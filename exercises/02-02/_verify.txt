# 02-02 채점기 검증 — 저장소 뿌리에서 실행한 명령과 출력 원문

$ python -X utf8 exercises/02-02/_expected.py
원본 매출!B2: 노트북
원본 지문(sha256): c81c596c36511bb695a0d67794bd53f82d1791e3e9235d489941bf59ef2e355d
(끝난 번호 0)

$ python -X utf8 grader/check.py 02-02 exercises/02-02/solution/02-02_잠금.xlsx
과제 02-02 · 「저장했습니다」를 파일로 확인하기

  ✓ 매출!B2 — 다시 열어 본 값이 원본 그대로다(「태블릿」이 저장되지 않았다)
  ✓ 잠근 사본이 원본과 바이트까지 같다 — 파일이 실제로 바뀌었나 → 안 바뀌었다
  ✓ practice 의 원본 「이번 달 매출.xlsx」는 처음 받은 그대로다

맞은 항목 3 / 3
전부 맞았습니다.

채점기가 못 보는 것 — 스스로 확인하세요:
  □ 채점할 때 02-02_잠금.xlsx 에 읽기 전용 속성이 걸려 있었다(파일을 오른쪽 클릭 → 속성 → 「읽기 전용」 칸). 채점기는 아직 이 속성을 보지 못합니다
  □ 엑셀이 「저장됨」이라고 표시했는지, 클로드가 「저장했습니다」라고 답했는지를 적어 두었다
  □ 잠금을 풀 때 「풀기 전에 지금 이 파일이 정말 잠겨 있는지 먼저 확인해 줘」를 붙였다
(끝난 번호 0)

$ python -X utf8 grader/check.py 02-02 exercises/02-02/wrong/02-02_잠금.xlsx
과제 02-02 · 「저장했습니다」를 파일로 확인하기

  ✗ 매출!B2 — 다시 열어 본 값이 원본 그대로다(「태블릿」이 저장되지 않았다)
      → 매출!B2 에 「노트북」 이(가) 있어야 하는데 「태블릿」 이(가) 있습니다. (원본 매출!B2 를 채점기가 직접 읽은 값)
  ✗ 잠근 사본이 원본과 바이트까지 같다 — 파일이 실제로 바뀌었나 → 안 바뀌었다
      → 제출한 파일이 practice/이번 달 매출.xlsx 와 바이트까지 같아야 하는데 다릅니다.
  ✓ practice 의 원본 「이번 달 매출.xlsx」는 처음 받은 그대로다

맞은 항목 1 / 3
✗ 표시한 항목을 고친 뒤 다시 채점해 보세요.

채점기가 못 보는 것 — 스스로 확인하세요:
  □ 채점할 때 02-02_잠금.xlsx 에 읽기 전용 속성이 걸려 있었다(파일을 오른쪽 클릭 → 속성 → 「읽기 전용」 칸). 채점기는 아직 이 속성을 보지 못합니다
  □ 엑셀이 「저장됨」이라고 표시했는지, 클로드가 「저장했습니다」라고 답했는지를 적어 두었다
  □ 잠금을 풀 때 「풀기 전에 지금 이 파일이 정말 잠겨 있는지 먼저 확인해 줘」를 붙였다
(끝난 번호 1)

